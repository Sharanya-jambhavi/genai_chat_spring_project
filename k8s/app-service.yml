apiVersion: v1
kind: Service
metadata:
  name: genai-app-service
  namespace: genai-project
spec:
  type: NodePort
  selector:
    app: genai
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30088
      